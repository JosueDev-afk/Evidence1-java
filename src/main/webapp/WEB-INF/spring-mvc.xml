<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Escaneo de componentes limitado a controladores para evitar parseo de clases no necesarias -->
    <context:component-scan base-package="org.example.evidence1.controller"/>

    <!-- Habilitar anotaciones MVC -->
    <mvc:annotation-driven />
    <!-- Permitir que el servlet por defecto sirva recursos estáticos y JSP fuera de controladores -->
    <mvc:default-servlet-handler/>

    <!-- Resolver de vistas JSP bajo WEB-INF/views (no accesibles directamente) -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- Mapeo directo de vista para /user -->
    <mvc:view-controller path="/user" view-name="register"/>

    <!-- Ruta de prueba para validar view-controller -->
    <mvc:view-controller path="/ping" view-name="register"/>

</beans>